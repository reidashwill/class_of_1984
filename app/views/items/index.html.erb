<h2>Items</h2>
  <% if @items.any? %>
    <ul>
      <% @items.each do |items| %>
        <li>
        <%= link_to item.name, item_path(item) %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no items yet</p>
  <% end %>
